import type { ReactNode } from "react";

export const metadata = { title: "Admin — RUT Shop" };

export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
